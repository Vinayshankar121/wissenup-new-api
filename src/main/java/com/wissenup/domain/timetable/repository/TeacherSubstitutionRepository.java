package com.wissenup.domain.timetable.repository; import com.wissenup.domain.timetable.entity.TeacherSubstitution; import org.springframework.data.jpa.repository.JpaRepository; import java.time.LocalDate; import java.util.*;
public interface TeacherSubstitutionRepository extends JpaRepository<TeacherSubstitution,Long>{List<TeacherSubstitution> findAllByOrganizationIdOrderBySubstitutionDateDesc(Long org);Optional<TeacherSubstitution> findBySubstitutionIdAndOrganizationId(Long id,Long org);Optional<TeacherSubstitution> findByTimetableDetailIdAndSubstitutionDate(Long detail,LocalDate date);boolean existsByTimetableDetailIdAndSubstitutionDateAndStatusIgnoreCaseAndSubstitutionIdNot(Long detail,LocalDate date,String status,Long id);}
